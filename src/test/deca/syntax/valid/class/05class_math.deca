// Test écriture de méthode

class Math {
    int fastExp(int x, int n) {
        if (n == 0) {
            return 1;
        } else if (n % 2 == 0) {
            return fastExp(x * x, n / 2);
        } else {
            return x * this.fastExp(x * x, (n - 1) / 2);
        }
    }
}
