class Math {
    int max(int x, int y) {
        if (x >= y) {
            return x;
        } else {
            return y;
        }
    }
}

{
    int x = 714;
    int y = 589;

    Math m = new Math();
    println(m.max(x, y));
}